name: p0_mobile_lab
description: Shared Flutter shell for fast mobile experiments.
publish_to: "none"
version: 0.0.2+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
